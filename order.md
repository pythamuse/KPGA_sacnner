# 주문: 검수 화면 "경합" 배지 — 2등 칸에 실제 표시 구조가 있을 때

## 배경

스캔 경로의 남은 오답 13칸을 인식 쪽에서 잡는 다섯 갈래가 전부 측정으로 기각됐다
(Task/FIELD_TEST_2026-08-21.md §28.6–§28.9). 이번 라운드는 인식이 아니라 **표시 계층**이다:
값도 신뢰도도 빈칸 수도 바꾸지 않고, 검수자의 눈이 위험한 칸에 먼저 가게 만든다.

측정된 근거 (세 세트 1,053개 고신뢰 자동입력):

- 신호: **2등(패배한) 후보 상자의 `matchedScore` ≥ 0.01** — "진 칸에도 구조적 표시 흔적이
  있다". 잉크 총량이 아니라 구조 점수라는 것이 핵심이다. 경계 번짐은 잉크는 있어도 구조가
  없다.
- 성능: 배지 137개(학생-세트당 2.4개, 세트별 1.8~3.2로 안정) · 오답 18칸 중 12칸 커버 ·
  무작위 대조 p99=6 대비 실측 12
- 특히 **취소 X 오답(p16) 10/10 전부 커버** — 그 학생 화면에서 중독된 다섯 칸에 모두 배지가
  달린다

## 작업

### 1. `matchedScore`를 2등 상자에 한해 항상 계산

지금 `matchedScore`는 `analyzeResidualComposition`(markDensity.ts ~2857) 안에서 **트레이스
중에만** 계산된다. 주석에 "Measured only while tracing"이라고 명시돼 있다.

- `matchedScore` 계산 부분(softPage/softBase 정합 잔차, ~2915–2946)을 **재사용 가능한
  헬퍼로 추출**하라. 트레이스와 배지가 **같은 정의**를 공유해야 한다 — 두 벌로 갈라지면
  계측기가 거짓말하게 된다.
- 프로덕션 경로에서는 그룹이 **고신뢰(자동입력)로 확정될 때, 2등 후보 하나에 대해서만**
  호출한다. 전 상자·전 결과에 대해 돌리지 마라 — 비용을 §7의 예산 안에 유지한다.
- 이미 트레이스가 켜져 있으면 중복 계산하지 말고 있는 값을 재사용해도 좋다(선택).

### 2. 상수와 플래그

```ts
/** 근거: FIELD_TEST §29 (2026-08-29). 세 세트 1,053칸에서 배지 137개로 오답 12/18
 *  커버(무작위 p99=6). 취소 X 오답 10/10 포함. 값·신뢰도·빈칸 수는 절대 바꾸지 않는다. */
export const CONTESTED_RUNNERUP_MSCORE = 0.01;
```

그룹 분석 결과에 `contested: boolean` 을 추가한다. **오직** 고신뢰 자동입력에서만 true가
될 수 있다(저신뢰·빈칸은 이미 검수 대상이라 배지가 무의미하다).

### 3. 인식 초안 → API → 검수 UI로 전달

- `RecognitionDraft` 의 필드별 메타데이터(기존 `recognitionValueSource` 류의 Record 패턴을
  따르라)에 `contested` 를 싣는다.
- `src/components/RecognitionReview.tsx` 의 필드 행에서, `자동 인식` 배지(~547행 스타일 맵)
  **옆에** 추가 배지를 단다:
  - 라벨: `경합`
  - 색: 기존 팔레트와 구별되는 주의색(기존 warn 계열 재사용)
  - 툴팁/보조문: `다른 칸에도 표시 흔적이 있습니다 — 지운 표시인지 확인해주세요`
- **하지 말 것**: 확정 상태 변경, `확인 필요` 카운트 편입, 자동 스크롤·포커스, 값 변경.
  배지는 순수 시각 신호다.

### 4. 트레이스

트레이스 문자열에 `contested=1` 을 추가한다 (해당 시에만).

### 5. 시험

- 단위: `contested` 판정 — 문턱 경계(0.0099 vs 0.0100), 고신뢰가 아닐 때 항상 false,
  2등이 없을 때(단일 후보) false.
- 렌더: 기존 검수 렌더 시험 패턴(`RecognitionReview` 관련 시험이 이미 있다)에 맞춰
  contested 필드가 배지를 그리는지 / 없으면 안 그리는지.
- 기존 전체 통과: `npx vitest run`, `MARK_AFFINE_TONE=1 npx vitest run`, `npx tsc --noEmit`,
  `npm run build`.

## 절대 조건

1. **값·신뢰도·빈칸 무변경.** 채점 숫자에 닿는 코드는 한 줄도 바꾸지 마라. `contested`는
   추가 메타데이터일 뿐이다. (위임자가 세 세트 노드 총계 바이트 동일로 재검증한다.)
2. **성능**: matchedScore 추가 계산은 고신뢰 그룹당 1회(2등 상자만).
3. **자체 합격 판정 금지.** 이 체크아웃에는 정답표도 스캔도 없다. 배지가 실제 오답 위에
   달리는지는 위임자가 중앙에서 측정한다.

## 완료 시

`git add -A && git commit` (브랜치 `codex-contested-badge`), 푸시 금지. `report.md`:
바꾼 파일:행, matchedScore 헬퍼 추출 방식, 메타데이터 경로, UI 위치, 시험 결과 전문,
명세와 다르게 한 것 전부.
