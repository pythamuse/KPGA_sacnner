# 주문: 종이 경계 검출의 밝기 문턱을 이미지에서 유도하도록

## 확정된 원인 (재조사 금지 — 측정 완료)

사진 경로의 오답 7건이 전부 `cagi.q01 → "3"` 이다. 사슬은 이렇다:

1. `src/lib/recognition/markDensity.ts:651` `detectPaperBounds` 가 **절대 밝기 문턱
   `[195, 170]`** 으로 "밝은 종이 성분"을 찾는다.
2. 보정된 사진의 종이는 중앙값(p50)이 **167** 근처다. 170 바로 아래라 종이가 반으로 쪼개져
   성분이 안 잡힌다 → `pageBounds = null`.
3. `applyTemplateRegistrationFrame` 이 `no-page-bounds` 로 **프레임 좌표**로 떨어진다.
   `detectFrameBounds` 는 어두운 책상 테두리 행까지 "프레임"으로 잡아 시트+책상 전체에
   템플릿을 펼친다.
4. 템플릿 행이 헤더행 위에 얹히고, 헤더 텍스트가 가장 빽빽한 4열(`거의 항상 있다`)이 잔차를
   이겨 "3"이 나온다. 이때 이긴 상자는 `inkInvariant=zeroed`(빈 양식보다 옅음) — 잡음이다.

전수 확인: 76쪽에서 **`pageBounds` 발견 ⇔ p50 ≳ 170** 이 정확히 성립한다.

| 세트 | pageBounds 발견 | 종이 p50 | 오답 |
|---|---|---|---|
| OLD(대조군) | 19/19 | 197~207 | 0 |
| Set1 | 3/19 | 166~170 | 6 (전부 실패 쪽) |
| Set2 | 11/19 | 실패 쪽 115~140 | 4 |
| Set3 | 18/19 | 170~180 | 1 |

**인과 확인**: 화소를 선형으로 밝혀(p90→230) `detectPaperBounds` 만 재실행하면
Set1 3/19 → **19/19**, Set3 18 → 19, OLD 19 → 19. Set2는 11 → 13 (그늘진 6쪽은 선형 이득으로
안 올라옴 — 상대 문턱이 필요한 이유).

이것은 `tableGridDetection` 의 절대 문턱(§16.7)과 같은 부류의 결함이다.

## 작업

`detectPaperBounds` 의 밝기 문턱을 상수 `[195, 170]` 이 아니라 **그 이미지의 관측값**에서
유도한다.

1. 후보 문턱을 이미지 히스토그램에서 구한다. 사진에는 두 무리가 있다 — 어두운 배경(책상)과
   밝은 종이. 그 사이에 문턱을 놓아야 한다(Otsu, 또는 배경 p-분위와 종이 p-분위의 중간 등).
   방식은 맡기되 **근거를 report에 쓰라**. 기존처럼 두 단계 후보를 유지해도 좋다(예: 유도값과
   그보다 낮은 보조값).
2. `findLargestBrightComponent` / `isPlausiblePaperBounds` 의 기하 조건(55%/62%/종횡비)은
   손대지 마라. 이 라운드는 **밝기 문턱 하나**다.
3. **스캔에서 결과가 바이트 단위로 같아야 한다.** 스캔은 종이가 255, 배경도 255(여백)라
   유도 문턱이 무엇이든 지금과 같은 성분이 나와야 한다. 유도 문턱이 스캔에서 195보다 크게
   벗어나면 그 자체가 설계 오류다 — 그럴 경우 195를 상한으로 두는 식으로 스캔 동작을 고정하라.
4. 트레이스/진단에 유도된 문턱이 보이게 하라(`contentBoundsRejection` 옆이나 `bounds=`
   문자열에). 계측기가 무엇을 썼는지 읽혀야 한다.

## 절대 조건 — 두 갈래

- **스캔 3세트 바이트 동일**: `355/7/75` · `343/8/86` · `342/3/92`
- **사진 4세트: 오답 비증가, 정답 비감소.** 기준 — `OLD 59/0` · `Set1 22/6` · `Set2 13/4` ·
  `Set3 24/1`. 특히 **`cagi.q01 → "3"` 오답 7건이 오답으로 남으면 실패**다(정답이 되거나
  빈칸이 되어야 한다). 어느 세트든 오답이 늘면 기각.
- `npx tsc --noEmit`, `npx vitest run`, `MARK_AFFINE_TONE=1 npx vitest run` 통과.
  **`npm run build` 는 돌리지 마라** — 메인 체크아웃에서 dev 서버가 돌고 있어 `.next` 가
  충돌한다(CLAUDE.md §4). 위임자가 나중에 돌린다.

위임자가 7개 조건을 중앙에서 재측정한다.

## 하지 말 것

- 화소를 밝히지 마라. 인과 확인의 선형 이득은 **진단**이었지 처방이 아니다. 화소가 바뀌면
  채점기가 보는 잉크량이 달라져 별개 회귀가 생긴다. 문턱만 옮긴다.
- `detectFrameBounds`, 격자 검출, 채점, 게이트에 손대지 마라.
- 자체 합격 판정 금지 — 이 체크아웃에는 정답표·스캔·사진이 없다. 합성 시험까지가 이
  작업이다.

## 시험

`tests/` 에 단위 시험: (a) 밝은 종이(230)+어두운 책상(40) 합성 → 종이 경계 발견,
(b) 어두운 종이(p50≈165)+책상(40) → **발견**(이것이 회귀 시험), (c) 전면 흰색(스캔 흉내,
255) → 기존과 같은 경계, (d) 종이 없이 균일한 어두운 이미지 → null. 기존 시험 전부 통과.

## 완료 시

`git add -A && git commit` (브랜치 `codex-paper-bounds`), 푸시 금지. `report.md`: 바꾼
파일:행, 유도 방식과 근거, 스캔 고정 방식, 합성 시험 결과 전문, 명세와 다르게 한 것과
이유, 확신 없는 부분.
