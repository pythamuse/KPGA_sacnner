# 취소 표시 거부권 — 문턱 0.7이 §5.4 한도 안에 든다 (2026-09-03)

**표적**: 남은 오답 9칸은 전부 두 칸에 잉크가 있는 경합이고, 그중 다수는 학생이 한 칸에 표시했다가 X로 지운 자리다([FIELD_TEST](FIELD_TEST_2026-08-21.md) §34.10).
**작성**: 오퍼스 서브에이전트. 계측 `contest-shape` `f6c12b5`(형태 특징 8종, 트레이스 전용), 거부권 `cancel-veto` `a1d0a34`(4중 게이트 통과 뒤
`crossingScore ≥ 0.6`(기본) **이고** `inkBboxFill ≥ 0.22`면 자동값을 거절하고 경합으로 표시; 2위를 대신 채우지 않음). 시험 504 통과, off 바이트 동일(md5).
**판정**: 메인 에이전트.

## 1. 절차 사고 하나 — off를 `=0`으로 준 판정

첫 판정에서 off/on이 완전히 같은 수치로 나왔다. 원인은 코드가 아니라 판정 스크립트였다: `markDensity.ts`의 opt-in 플래그는 `Boolean(process.env.X)`로
검사하므로 **`MARK_CANCEL_VETO=0`은 거부권을 켠다.** 스캔 8회·사진 8회를 헛돌렸다. 변수를 비우고 다시 재니 네 세트 모두 main과 정확히 일치
(`361/5/71 · 346/8/83 · 348/3/86 · 307/9/121`) — 계측 커밋은 중립이다. CLAUDE.md §2에 못 박았다.

## 2. 문턱 곡선 (칸 단위, 한 번의 덤프로)

문턱마다 하네스를 다시 돌리는 대신, 스캔 PDF를 제품 설정(1.5배·0.86)으로 쪽 JPEG로 렌더하고(`.tmp/render-scan-pages.cjs`) `MARK_SHAPE_TRACE=1`로
후보 형태를 덤프한 뒤, **하네스가 실제로 자동 채운 칸**과 짝지어 문턱을 훑었다(`scratchpad/veto-sweep.py`). 0.6에서의 예측이 실제 하네스 델타와
칸 수까지 일치해(세트 1 −17 정답·−3 오답) 곡선을 신뢰할 수 있다.

| 표본 | 0.6: 오답↓ / 정답↓ / 비율 | **0.7: 오답↓ / 정답↓ / 비율** | 0.8 |
|---|---|---|---|
| 스캔 세트 1 | 3 / 17 / 6.7:1 | **1 / 7 / 8.0:1** | 0 / 6 (순손실) |
| 스캔 세트 2 | 1 / 17 / 18.0:1 | **1 / 8 / 9.0:1** | 0 / 4 |
| 스캔 세트 3 | 1 / 13 / 14.0:1 | **1 / 5 / 6.0:1** | 0 / 1 |
| **세 세트 합산(§5.4)** | **5 / 47 → 10.4:1 ✗** | **3 / 20 → 7.7:1 ✓** | — |
| 스캔 세트 4 (회색조) | 2 / 23 / 11.5:1 | **2 / 10 / 6.0:1** | 0 / 2 |
| 브라우저 19명 (래스터) | 4 / 14 / 3.5:1 | **3 / 7 / 3.3:1** | 2 / 4 |

`inkBboxFill` 문턱을 0.30으로 올리면 어느 배율에서도 오답이 하나도 안 빠진다(순손실) — 0.22를 유지한다.

**즉 채택 가능한 설정은 `crossing ≥ 0.7 & fill ≥ 0.22` 하나다.** 코드 기본값 0.6은 §5.4 한도를 넘으므로 병합 시 기본값을 0.7로 올려야 한다.

## 3. 사진과 브라우저 (문턱 0.7)

사진 4세트(제품 입력): OLD 61→**60**/0 · Set1 34→34/0 · Set2 42→42/0 · Set3 32→32/0 — **오답 0 유지, 비용은 한 칸.**

브라우저 19명(세트 1, dev, `MARK_CANCEL_VETO=1 MARK_CANCEL_CROSSING=0.7`):

| | 기준선(main) | 거부권 0.7 |
|---|---|---|
| 자동 입력 | 346 | 335 |
| 자동 정답 | 337 | 329 |
| **자동 오답** | **9** | **6** |
| 미확정 빈칸 | 91 | 102 |

셀 diff 11칸: **오답→빈칸 3**(p8 q07·q08 = 척도표의 X 취소, p16 q05), 정답→빈칸 8(p7·p12 cagi.q05, p8·p18·p19 cagi.q01, p13 cagi.q08,
p19 basic.schoolType, p19 satisfaction.q01). 확인 필요 1칸당 오답 0.27칸 = **3.7:1**. 거부권은 값을 뺏기만 하므로 **새 오답은 구조적으로 불가능**하고
실측도 그렇다.

## 4. 판정 — 채택 (문턱 0.7)

§5.4 예외 세 조건 모두 충족: 브라우저 오답 감소(9→6), 값을 뺏기만 하는 변경, 세 세트 합산 7.7:1 ≤ 10. 사진 오답 0 유지(비용 1칸), 회색조 세트는
오답 2 제거(6:1). 기본값을 0.7로 올린 뒤 병합한다.

남은 오답 6칸: p15 q04, p16 q02·q03·q04·q06, p19 q09 — 교차 점수가 0.29 이하라 이 특징으로는 못 가른다(§34.10).

## 5. 병합과 함께 남는 결함 (다음 라운드)

거절된 행은 값이 없으므로 `contestedUnconfirmedFields`가 건너뛴다 → **경합 카드가 뜨지 않고 "취소 표시로 보이는 교차 획" 문구가 검수자에게 닿지
않는다**(서브에이전트 보고 §2). 칸은 빈칸이라 사람이 채우게 되므로 안전한 방향이지만, 왜 비었는지 모르면 검수가 느려진다. 검수 UI·정착 계층
한 라운드가 필요하다 — 값 경로는 건드리지 않는다.

## 6. 병합 (2026-09-03)

`89f0eab`(병합) + `212787b`(기본 문턱 0.7) + `59adf9f`(**거부권 기본값 on**, `MARK_CANCEL_VETO=0`이 이전 동작). 기본값을 뒤집은 이유는 판정이 통과했고
플래그가 opt-in인 채로는 배포해도 프로덕션 동작이 바뀌지 않기 때문이다 — 밴드 라운드와 같은 절차다.

환경변수 없이(=프로덕션 설정) 재확인: 스캔 세트 1 `354/4/79`, 사진 OLD `60/0`, 브라우저 19명 `자동 335 · 정답 329 · 오답 6 · 빈칸 102` —
플래그를 명시로 켠 판정 실행과 **셀 차이 0**. 시험은 기본(=on)과 `MARK_CANCEL_VETO=0` 양쪽에서 504 통과.

새 기준선은 CLAUDE.md §3.1·§5.2에 반영했다.

**절차 예외 하나**: 문턱 기본값을 올리는 한 줄 변경을 맡은 서브에이전트가 서버 과부하(HTTP 529)로 두 번 연속 편집 전에 종료됐다. 배포를 한 줄 때문에
세울 수 없어 판정자(메인)가 직접 바꾸고 같은 세 명령으로 확인했다. 기본값 on 전환도 같은 이유로 판정자가 했다. 위임 규칙(§0)의 예외이며 근거는 이 절이다.

