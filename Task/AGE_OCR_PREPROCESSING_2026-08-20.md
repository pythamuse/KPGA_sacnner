# 작업 B — 연령대 OCR 전처리 (2026-08-20~)

연령대는 6페이지 **전부 빈칸**이다. 워커 교착은 이미 해소됐고 OCR은 매 페이지 실제로 실행되지만, 손글씨 숫자를 읽지 못한다.

상위 문서: [Docs/00_PRD.md §10-1](../Docs/00_PRD.md) — 자동 입력률이 제품 성립 조건이다.
선행 스레드: [[MEASUREMENT_RENDER_PARITY_2026-08-19]] 사이클 4, [[OCR_ANCHORED_ROW_DETECTION]]

---

## 1. 배경 — 반드시 먼저 읽을 것

### 1.1 이미 해결된 것 (다시 건드리지 마라)

콜드 스타트마다 5MB 영문 모델을 CDN에서 받다가 6초 예산을 넘기고, 그 뒤 모든 페이지가 `worker_pending` 가드에 걸리던 교착은 **`langPath` 지정으로 해소됐다**(`eng.traineddata`가 `src/lib/recognition/assets/`에 있고 배포 번들에 포함된다). 진단 문구가 `worker was still initializing`에서 실제 판정 결과로 바뀐 것이 그 증거다.

### 1.2 확정된 실패 지점

임시 계측으로 실제 OCR 출력을 확인했다.

| 페이지 | 정답 | OCR 텍스트 | confidence |
|---|---|---|---|
| p1 | 14 | **19** | 53 |
| p2 | 14 | **4** | 43 |
| p3 | 15 | (빈 문자열) | 0 |

`MIN_DIGIT_CONFIDENCE = 60`이 이 오답들을 막고 있다. **이 문턱을 낮추면 p1에 `19`가 자동 입력된다 — 절대 금지.**

### 1.3 해상도 가설은 기각됐다

`REAL_SCAN_RENDER_SCALE`을 1.5 / 2.0 / 3.0으로 올려 측정했으나 **셋 다 6/6 빈칸**이었다. 크롭을 키우는 방향은 답이 아니다.

### 1.4 확정된 원인 — 전처리

OCR 입력 이미지를 저장해 눈으로 확인한 결과 두 가지가 보였다.

1. **인쇄된 칸 밑줄이 크롭에 그대로 포함된다.** `getAgeDigitsRect`가 상하 12%만 잘라내는데 그것으로는 제거되지 않는다. Tesseract가 이 선을 글자의 일부로 보거나 레이아웃 분석이 무너진다.
2. **`threshold(190)` 고정 이진화가 얇은 획을 부순다.** p3의 `5`가 점 몇 개로 흩어져 빈 문자열이 됐다.

크롭 실측: `digitsCrop = 68×16px` (배포본 래스터 기준). 이를 4배 확대(최소 320×96)해 OCR에 넣는다.

---

## 2. 목표와 합격 기준 (착수 전 확정)

`REAL_SCAN_RENDER_SCALE` 기본값(배포본 래스터)에서 6페이지를 측정한다. 정답은 `14, 14, 15, 15, 12, 13`이다.

| # | 기준 |
|---|---|
| **필수** | **`WRONG = 0`.** 틀린 나이가 자동 입력되면 즉시 기각 |
| **필수** | 전체 `CORRECT ≥ 102` (현재 기준선) |
| **목표** | `basic.age` 자동 입력 **6칸 중 3칸 이상** 정답 |

빈칸은 검수자의 키 입력 한 번이지만, 틀린 나이는 사람이 확인한 것처럼 저장된다.

---

## 3. 방향 제안 (하나 이상을 조합하고 이유를 문서에 남길 것)

### 방향 1 — 인쇄 밑줄 제거 (권장, 근거가 가장 명확)

이미 `subtractTemplateDigits`가 빈 양식 기준 차감을 하고 있다. 그것으로 밑줄이 지워지지 않는다면:

- 크롭 안에서 **가로로 길게 이어진 어두운 행**(폭의 60% 이상)을 찾아 흰색으로 지운다. 숫자 획은 그렇게 길지 않다.
- 또는 밑줄 위치를 찾아 그 아래를 잘라낸다.

### 방향 2 — 적응형 이진화

`threshold(190)` 고정값 대신 크롭의 밝기 분포에서 문턱을 계산한다(Otsu 또는 백분위 기반). 흐린 스캔에서 획이 살아난다.

### 방향 3 — Tesseract 파라미터

숫자 전용으로 이미 영문 워커를 쓰고 있으나, `tessedit_char_whitelist = 0123456789`와 `tessedit_pageseg_mode`를 손글씨 두 자리에 맞게 조정하면 오독이 줄 수 있다.

---

## 4. 제약 (위반 시 되돌림)

1. **`MIN_DIGIT_CONFIDENCE`를 낮추지 마라.** 지금 오답을 막고 있는 유일한 방어선이다. 문턱을 낮춰 숫자를 올리는 것은 이 작업의 실패다.
2. **소유 파일**: `src/lib/recognition/ocrTextLines.ts`. **이 파일 외의 소스 파일을 수정하지 마라** — 다른 작업이 `basicCheckboxDetection.ts` · `detectCheckmarks.ts` · `markDensity.ts`에서 동시에 진행 중이다. 크롭 영역 조정이 필요하면 `getAgeDigitsRect`(`detectCheckmarks.ts`)를 고치는 대신 `ocrTextLines.ts` 안에서 받은 크롭을 추가로 좁혀라.
3. **`tests/real-scan-measure.test.ts`, `local-scans/`, `templates/`, `src/lib/recognition/assets/`를 수정하지 마라.**
4. 필드명이나 페이지 번호로 분기하지 마라.
5. `npm test`·`npm run build` 통과. 특히 `tests/age-ocr.test.ts`가 통과해야 한다. 버전 마커는 갱신하지 마라(병합은 지휘자가 한다).

---

## 5. 보고에 반드시 포함할 것

- (a) 선택한 방향과 그 이유
- (b) 파일별 변경 내용
- (c) `npm test` / `npm run build` 결과
- (d) `MIN_DIGIT_CONFIDENCE`를 낮추지 않았다는 확인
- (e) 소유 파일(`ocrTextLines.ts`) 밖을 수정하지 않았다는 확인

**`CORRECT`/`WRONG` 측정은 하지 않아도 된다.** 이 worktree에는 정답표와 실제 스캔 PDF가 없다. 판정은 지휘자가 중앙에서 수행한다.

---

## 6. 사이클 기록

### 사이클 1 — 전처리 (커밋 `4d510bb`)

인쇄 밑줄 제거 + 히스토그램 기반 이진화(Otsu) + 획 분리 + 2중 리더(전체 박스 `SINGLE_WORD` / 낱자 `SINGLE_CHAR`).

| 항목 | 결과 |
|---|---|
| 합성 벤치 | 0/6 → **6/6** |
| **실제 스캔 `basic.age`** | 0/6 → **0/6 (무변화)** |
| 전체 | `CORRECT 102/135, WRONG 0` (기준선과 동일) |

합성에서 완벽했으나 실제에서 0. **[[15_FAILURE_ELIMINATION_TABLE]] G9(합성 판정 불가)의 두 번째 사례**다.

다만 실패 지점이 이동했다. 이전에는 빈 문자열·잡문자였고, 이제는 파이프라인이 끝까지 돌아 **마지막 관문에서만** 걸린다.

### 사이클 2 — 계측 (커밋 `e59bd1a`, `main` 병합 `7db115a`)

튜닝 없이 진단 문구만 확장했다. 거부한 관문 이름 + 두 리더의 판독값·신뢰도 + 크롭 기하를 모두 노출한다. 병합 전 실측으로 **`CORRECT 102/135, WRONG 0` — 완전 무변화**를 확인했다(계측 전용이라는 보고와 일치).

판독값은 `0-9`로 걸러 기록하므로 스캔 내용이 문자열로 새지 않는다.

---

## 7. 실측 — 6페이지 전수 (정답 `14, 14, 15, 15, 12, 13`)

### 7.1 배포본 래스터 (scale 1.5, 크롭 68×16 → 작업 이미지 408×96)

| p | 정답 | 관문 | 전체 박스 | 낱자 | 획 (WxH) | 잉크‰ |
|---|---|---|---|---|---|---|
| 1 | 14 | `whole-box-below-confidence` | `"1"` c0 | `""` | 20x84, **408x92** | 166 |
| 2 | 14 | `whole-box-unreadable` | `""` | `"4"` c0 | 16x63, **207x83** | 166 |
| 3 | 15 | `no-number-read` | `""` | 생략 | 28x38, **408x80**, 40x35 | 128 |
| 4 | 15 | `no-number-read` | `""` | `""` | 20x74, 35x67 | 58 |
| 5 | 12 | `readers-disagreed` | **`"12"` c39** | `"2"` c59 | **408x92** (1개) | 182 |
| 6 | 13 | `whole-box-unreadable` | `""` | `"4"` c0(33,0) | 70x96, 115x96 | 151 |

### 7.2 3배 해상도 (scale 3.0, 크롭 138×34 → 작업 이미지 552×136)

| p | 정답 | 관문 | 전체 박스 | 낱자 | 획 (WxH) | 잉크‰ |
|---|---|---|---|---|---|---|
| 1 | 14 | `whole-box-unreadable` | `""` | `"4"` c0**(0,92)** | 21x118, 97x128 | 147 |
| 2 | 14 | `whole-box-unreadable` | `""` | `"4"` c0(12,0) | 20x93, **552x111** | 110 |
| 3 | 15 | `no-number-read` | `"0"` c0 | 생략 | 42x55, 29x104, 54x47 | 110 |
| 4 | 15 | `no-number-read` | `""` | `""` | 25x105, 46x96 | 52 |
| 5 | 12 | `readers-disagreed` | `"2"` c10 | `"1"` c0**(51,0)** | 241x125, 85x102 | 148 |
| 6 | 13 | `no-number-read` | `""` | `""` | 25x129, 146x123 | 115 |

---

## 8. 이 측정으로 확정된 것

| # | 사실 | 근거 |
|---|---|---|
| B1 | **해상도는 원인이 아니다 — 전처리를 넣은 뒤에도 그렇다** | 1.5와 3.0 모두 0/6. 3.0에서는 p5의 정답 `"12"`마저 잃었다 |
| B2 | **리더는 빈 문자열을 낸다. 유사 문자를 내는 것이 아니다** | 진단이 `nondigits=N`을 붙이도록 되어 있는데 12개 판독 어디에도 나타나지 않음 |
| B3 | **낱자는 실제로 높은 신뢰도로 읽힌다** | p1@3.0 둘째 자리 `4` **c92**, p5@3.0 첫 자리 `1` c51, p5@1.5 전체 박스 `"12"` c39 |
| B4 | **항상 두 자리 중 하나만 침묵한다** | 낱자 신뢰도쌍이 `(0,92)`·`(12,0)`·`(51,0)`·`(33,0)` — 한쪽은 0 |
| B5 | **인쇄 테두리가 살아남아 전 폭 성분이 된다** | 1.5에서 4/6 페이지, 3.0에서 2/6 페이지에 작업 이미지 **전체 폭**(408·552) 성분. p5@1.5는 이미지 전체가 성분 1개 |
| B6 | 신뢰도는 낱자 최솟값이다 | `Math.min` — 한 자리가 침묵하면 쌍 전체가 거부된다. **설계대로다. 유지한다** |

## 9. 이 측정으로 기각된 것

| 가설 | 결과 |
|---|---|
| 해상도를 올리면 읽힌다 | **기각 2회.** 전처리 전(1.5/2.0/3.0), 전처리 후(1.5/3.0) 모두 0/6 |
| `tessedit_char_whitelist`가 LSTM에서 무시되어 `l`·`|`로 나온다 | **기각.** B2 — 애초에 아무 문자도 나오지 않는다. 유사 문자 매핑(`l`→`1`)은 **시도할 필요가 없다** |
| 크롭·밑줄·이진화가 남은 장애물이다 | **기각.** B3 — 같은 파이프라인에서 c92가 나온다 |
| 워커·타임아웃 문제 | **기각.** 두 리더 모두 매 페이지 완주 |

## 10. 남은 후보 (우선순위)

1. **작업 이미지의 전 폭·전 높이를 차지하는 성분 제거** ← B5. 두 자리 숫자 칸 안의 손글씨는 칸 전체만큼 넓을 수 없다. 필드명이 아니라 기하로 정의되는 일반 규칙
2. 침묵하는 자리와 획 폭의 상관 확인 — 진단이 획과 낱자 신뢰도를 **같은 순서로** 짝지어 출력해야 판정 가능
3. Tesseract `SINGLE_CHAR`가 세로 막대 하나에 침묵하는 문제 — 렌더 여백·획 높이(`DIGIT_OCR_STROKE_HEIGHT=48`) 조정

## 11. 남겨둔 부채

진단 문구는 검수 화면에 **그대로 노출된다**(`RecognitionReview.tsx:529`). 지금은 사용자가 보내는 화면 캡처에서 그대로 읽히므로 진단에 유리하지만, **실무 배포 전에는 대괄호 기술 블록을 감춰야 한다.**

---

## 12. 사이클 3 — 프레임 제거 + 획·판독 짝짓기 (미병합)

브랜치 `worktree-agent-ab4bae8aa124836cd`. 두 가지를 넣었다.

1. **괘선 제거를 기울기 적합으로 교체.** 기존 `eraseHorizontalRules`는 *한 행 안에서* 폭의 55% 연속 잉크를 요구했다. 스캔 괘선은 축에 정확히 나란하지 않고, 크롭을 6배 확대하므로 페이지에서의 0.5px 기울기가 작업 이미지에서 ~3px가 된다 — 괘선이 136px 토막으로 쪼개져 224px 검사를 통과하지 못했다. **이것이 B5의 기계적 원인이다.**
2. **작업 이미지 폭의 90% 이상을 차지하는 성분 제거**, 제거 후 남는 것이 없으면 원본 잉크로 재판독(`read=raw-ink`).

### 12.1 실측 — 획과 그 획의 판독값을 짝지어 출력

| p | 정답 | 좁은 획 | 판독 | 넓은 획 | 판독 | 프레임 |
|---|---|---|---|---|---|---|
| 1 | 14 | 20x84 | `""` c0 | 77x96 | `""` c0 | 408x92 **제거됨** |
| 2 | 14 | 16x63 | `""` c0 | 72x83 | **`"4"` c90** | 207x83 → 72x83 |
| 3 | 15 | — | 획 3개라 낱자 생략 | — | — | 408x80 → 22x84 |
| 4 | 15 | 20x74 | `""` c0 | 35x67 | `""` c0 | **무변화 (대조군 유지)** |
| 5 | 12 | 22x96 | `""` c0 | 64x88 | `""` c0 | 408x92 1개 → 2개로 **분리** |
| 6 | 13 | 70x96 | `"4"` c33 | 115x96 | `""` c0 | 무변화 |

**자동 입력 0/6 — 무변화.** 전체 `CORRECT 102/135, WRONG 0`.

### 12.2 이 사이클로 확정된 것

| # | 사실 | 근거 |
|---|---|---|
| B7 | **프레임 제거는 기계적으로 성공했다** | 408·207 → 77·22·64. p4는 예측대로 한 픽셀도 변하지 않음(대조군 유지) |
| B8 | **폭 16~22px 획은 단 한 번도 읽히지 않는다** | p1·p2·p4·p5의 좁은 획 4개 전부 `""` c0. 정답 6개가 **모두 1로 시작**한다 |
| B9 | **그런데 둘째 자리도 대부분 읽히지 않는다** | 77x96·35x67·64x88 모두 빈 문자열. 읽힌 것은 p2의 72x83(c90) 하나뿐 |
| B10 | **읽히지 않는 획은 작업 이미지 높이에 붙어 있다** | 실패 획의 높이 96·88·96·96 (이미지 높이 96). 성공한 p2는 83. **상하 괘선이 아직 획에 붙어 있다** |
| B11 | 프레임을 지우면 p5의 유일한 정답 판독도 사라진다 | 전체 박스 `"12"` c39 → `""`. 뭉친 성분이 오히려 단어로 읽히고 있었다 |

### 12.3 판정 — 미병합

기계적으로는 지시대로 동작하고 대조군도 지켰으나 **자동 입력 이득 0**이다. 프로젝트 규칙(이득 없는 변경은 병합하지 않는다)에 따라 `main`에 넣지 않고 브랜치로만 보존한다.

**B8 하나만 풀어도 소용이 없다는 것이 이 사이클의 핵심 결론이다.** 좁은 획을 기하로 `1`이라 단정하는 규칙(십진 글리프 중 폭/높이 ≤0.3인 것은 `1`뿐이다)을 넣더라도, 둘째 자리가 읽히는 페이지는 p2 하나뿐이므로 **6칸 중 1칸**만 회복된다. 오답 위험을 감수할 값이 아니다.

### 12.4 남은 후보 (우선순위 재조정)

1. **B10 — 획에 붙은 상하 괘선 제거.** 실패 획이 전부 이미지 높이에 닿아 있다. 이걸 풀어야 둘째 자리가 읽힌다. 이것이 풀리면 B8 규칙이 비로소 값을 갖는다
2. B8 — 좁은 획의 기하 판정 (1번 이후에만 의미 있음)
3. ~~해상도~~ · ~~유사문자 매핑~~ · ~~크롭~~ · ~~전 폭 성분 제거~~ → 전부 기각

### 12.5 방법론 실패 — 브랜치 참조로 측정하면 안 된다

첫 측정에서 이전 사이클과 **바이트 단위로 동일한** 출력이 나왔다. 하위 에이전트가 작업을 **커밋하지 않고** 워크트리에 남겨두었는데, 내가 `git show <브랜치>:<파일>`로 꺼내 측정했기 때문이다. 브랜치 참조는 병합 전 코드가 아니라 **작업 전 코드**였다.

→ **규칙: 파일을 바꿔치기한 뒤 반드시 `git diff --stat`으로 실제로 달라졌는지 확인하고 측정한다.** 계측기가 엉뚱한 것을 재고 있던 사례가 이것으로 두 번째다([[15_FAILURE_ELIMINATION_TABLE]] G8).
